# Root replacement

Previous root revision: `522b30fc264f7bc1900b13005aa1bbc6f4b914ff`.
Recovery branch: `codex/pre-living-tree-20260912`.

The original `mc/index.html` remains the source of the Minecraft page. The build copies it byte-for-byte into `dist/mc/index.html`. The old forum's root files are removed from the new deployment.

To recover the former site, deploy the original revision or the recovery branch. Do not reset an active working tree with uncommitted work.
